#!/usr/bin/env python
#
import os
import time
import sys
import socket
import struct

FTP_IP = "127.0.0.1"
FTP_PORT = 2121
BUFFER_SIZE = 1024

print("STARTING FTP SERVER")









def main():
    try:
        ftpd.serve_forev

if __name__ == "__main__":
    # FTP SERVER SETUP
    IP = '127.0.0.1'
    PORT = 21
    server = ServerFTP(IP, PORT)
    print('FTP Server - {}:{} \n'.format( IP, PORT))
